# Version 1.0.3

## Type
PATCH

## Summary of Changes
- Removed `develop` branch from workflow
- Updated AI_GIT_GOVERNANCE.md to use `main` only
- Simplified branching model: only `main` and version branches
- Updated workflow steps to merge directly to `main`
- Updated forbidden actions list
- Updated branch structure documentation

## Modified Files
- AI_GIT_GOVERNANCE.md

## Added Code

```diff
+ # 7️⃣ Mandatory Workflow (FOLLOW IN ORDER)
+
+ ### STEP 1: Sync main
+ ```bash
+ git checkout main
+ git pull origin main
+ ```

+ ### STEP 9: Merge to main and tag
+ ```bash
+ git checkout main
+ git merge <VERSION>
+ git tag -a v<VERSION> -m "Release <VERSION>"
+ git push origin main
+ git push origin v<VERSION>
+ ```

+ # 🌿 Branch Structure
+
+ ```
+ main ───── Production (with tags)
+   │
+   ├── 1.0.1 ─── Version branch (merged & tagged)
+   ├── 1.0.2 ─── Version branch (merged & tagged)
+   ├── 1.0.3 ─── Version branch (active)
+   ...
+ ```
```

## Removed Code

```diff
- ### STEP 1: Sync develop
- ```bash
- git checkout develop
- git pull origin develop
- ```

- - Commit directly to develop | Bypasses workflow

- Target:
- - develop (default)
- - main (major only)
```

## Impact Level
Medium (Workflow change)
