# Version 1.1.0

## Type
MINOR

## Summary of Changes
- Added Admin Dashboard (لوحة التحكم)
- Added statistics cards (surahs, ayat, reciters, tafsir sources)
- Added quick action buttons
- Added system information section
- Added admin view to store
- Added admin button to header
- Created admin stats API endpoint

## Modified Files
- src/app/page.tsx
- src/store/quran-store.ts
- src/components/quran/header.tsx
- VERSION

## Added Files
- src/components/quran/admin-panel.tsx
- src/app/api/admin/stats/route.ts

## Added Code

### admin-panel.tsx
```tsx
+ 'use client';
+
+ import { useState, useEffect } from 'react';
+ import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
+ ...
+
+ export function AdminPanel({ isOpen, onClose }: AdminPanelProps) {
+   // Dashboard component with stats cards
+   // Quick actions section
+   // System info section
+ }
```

### API endpoint
```tsx
+ export async function GET() {
+   const totalSurahs = await db.surah.count();
+   const totalAyat = await db.ayah.count();
+   const totalReciters = await db.reciter.count();
+   const totalTafsirSources = await db.tafsirSource.count();
+   return NextResponse.json(stats);
+ }
```

### Store update
```tsx
+ currentView: 'home' | 'quran' | 'duas' | 'admin';
+ setCurrentView: (view: 'home' | 'quran' | 'duas' | 'admin') => set({ currentView: view }),
```

### Header update
```tsx
+ {/* زر لوحة التحكم */}
+ <Button onClick={() => setCurrentView('admin')}>
+   <LayoutDashboard className="h-5 w-5" />
+ </Button>
```

## Removed Code
None

## Impact Level
Medium (New feature)
